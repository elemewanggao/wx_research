<view class="container">

  <view class="page-body">
    <view class="page-section">
      <view class="btn-area">
        <button type="primary" wx:if="{{canIUse}}" open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo">授权登录</button>
        <text wx:else>你的版本过低，请升级你的微信</text>
      </view>
    </view>
  </view>

</view>